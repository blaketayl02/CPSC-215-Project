package model;

// CPU part.
public class Cpu extends Parts {

    //MODIFIES: this
    //EFFECTS: creates a Cpu object with model, name, id, and price.
    public Cpu(String model, String name, int id, int price) {
        super(model, name, id, price);
    }

}
