package model;

// GPU part.
public class Gpu extends Parts {

    //MODIFIES: this
    //EFFECTS: creates a Gpu object with model, name, id, and price.
    public Gpu(String model, String name, int id, int price) {
        super(model, name, id, price);
    }

}
